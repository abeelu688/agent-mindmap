{
  "name": "agent-mindmap",
  "displayName": "Agent Mind Map",
  "description": "Read Cursor or Claude Code agent chat transcripts and visualize them as an interactive mind map.",
  "version": "0.1.1",
  "icon": "icon.png",
  "publisher": "airecorder",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/abeelu688/agent-mindmap.git"
  },
  "bugs": {
    "url": "https://github.com/abeelu688/agent-mindmap/issues"
  },
  "homepage": "https://github.com/abeelu688/agent-mindmap#readme",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "keywords": [
    "mind map",
    "ai",
    "agent",
    "transcript",
    "cursor",
    "claude code",
    "visualization",
    "knowledge graph"
  ],
  "galleryBanner": {
    "color": "#1e1e2e",
    "theme": "dark"
  },
  "activationEvents": [
    "onStartupFinished",
    "onCommand:agent-mindmap.openLatest",
    "onCommand:agent-mindmap.pickSession",
    "onCommand:agent-mindmap.analyzeAndMergeCurrentProject"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "agent-mindmap.openLatest",
        "title": "Agent Mind Map: Open Latest Session",
        "category": "Agent Mind Map"
      },
      {
        "command": "agent-mindmap.pickSession",
        "title": "Agent Mind Map: Choose Session…",
        "category": "Agent Mind Map"
      },
      {
        "command": "agent-mindmap.analyzeAndMergeCurrentProject",
        "title": "Agent Mind Map: Analyze All Sessions (Current Project)",
        "category": "Agent Mind Map"
      },
      {
        "command": "agent-mindmap.selectHost",
        "title": "Agent Mind Map: Select Host (Cursor/Claude Code)",
        "category": "Agent Mind Map"
      },
      {
        "command": "agent-mindmap.selectModel",
        "title": "Agent Mind Map: Select Model",
        "category": "Agent Mind Map"
      }
    ],
    "configuration": {
      "title": "Agent Mind Map",
      "properties": {
        "agentMindmap.host": {
          "type": "string",
          "enum": [
            "auto",
            "cursor",
            "claude-code"
          ],
          "default": "auto",
          "description": "Which AI product's on-disk transcripts to read. auto = detect from app name and existing transcript folders."
        },
        "agentMindmap.projectsDir": {
          "type": "string",
          "default": "",
          "description": "[Cursor] Override ~/.cursor/projects directory path."
        },
        "agentMindmap.claudeProjectsDir": {
          "type": "string",
          "default": "",
          "description": "[Claude Code] Override ~/.claude/projects directory path."
        },
        "agentMindmap.cursorStateDb": {
          "type": "string",
          "default": "",
          "description": "Override path to Cursor's globalStorage state.vscdb. Empty = platform default (Linux: ~/.config/Cursor/User/globalStorage/state.vscdb; macOS: ~/Library/Application Support/Cursor/User/globalStorage/state.vscdb; Windows: %APPDATA%/Cursor/User/globalStorage/state.vscdb). Used to read sidebar composer titles for human-readable session labels."
        },
        "agentMindmap.includeToolCalls": {
          "type": "boolean",
          "default": true,
          "description": "[Fallback view only] Include tool-call branches under each user question."
        },
        "agentMindmap.maxConclusionItems": {
          "type": "number",
          "default": 8,
          "minimum": 1,
          "maximum": 32,
          "description": "[Fallback view only] Maximum conclusion sub-nodes per question.",
          "deprecationMessage": "Used only when LLM summarization fails. Configure agentMindmap.maxTopics / maxItemsPerTopic for the default topic view."
        },
        "agentMindmap.autoRefresh": {
          "type": "boolean",
          "default": false,
          "description": "Watch the active transcript file and refresh the mind map."
        },
        "agentMindmap.llm.provider": {
          "type": "string",
          "enum": [
            "auto",
            "cursor-cli",
            "claude-cli"
          ],
          "default": "auto",
          "description": "LLM provider for topic extraction. auto follows agentMindmap.host (cursor-cli or claude-cli)."
        },
        "agentMindmap.llm.cliPath": {
          "type": "string",
          "default": "",
          "description": "Path to the headless CLI binary. Empty = auto-detect (Cursor: agent/cursor-agent; Claude: claude)."
        },
        "agentMindmap.llm.model": {
          "type": "string",
          "default": "",
          "description": "Optional model name passed to the CLI via --model. Empty uses the CLI default."
        },
        "agentMindmap.llm.timeoutMs": {
          "type": "number",
          "default": 480000,
          "minimum": 1000,
          "maximum": 600000,
          "description": "Hard timeout (ms) for a single CLI subprocess call. Total budget per summarisation is roughly timeoutMs * maxAttempts."
        },
        "agentMindmap.llm.maxAttempts": {
          "type": "number",
          "default": 1,
          "minimum": 1,
          "maximum": 10,
          "description": "Maximum number of cursor-agent attempts per summarisation. Retries are triggered by timeout / cli-failed / bad-json / bad-shape; cli-missing / cancelled / empty are not retried."
        },
        "agentMindmap.llm.retryBackoffMs": {
          "type": "number",
          "default": 1000,
          "minimum": 0,
          "maximum": 30000,
          "description": "Base backoff (ms) between retries. Effective wait is base * 2^(attempt-1) with ±25% jitter, capped at 10s."
        },
        "agentMindmap.maxTopics": {
          "type": "number",
          "default": 6,
          "minimum": 2,
          "maximum": 12,
          "description": "Target number of top-level topic nodes the LLM should produce."
        },
        "agentMindmap.maxItemsPerTopic": {
          "type": "number",
          "default": 6,
          "minimum": 1,
          "maximum": 16,
          "description": "Maximum sub-items (knowledge points / references) per topic."
        },
        "agentMindmap.cacheLlmResult": {
          "type": "boolean",
          "default": true,
          "description": "Cache the LLM topic graph keyed by transcript hash to avoid re-spending tokens. By default this is stored under storeDir/llm-cache; override via agentMindmap.llm.cacheDir."
        },
        "agentMindmap.llm.cacheDir": {
          "type": "string",
          "default": "",
          "description": "Path to the LLM cache directory. Empty = <storeDir>/llm-cache."
        },
        "agentMindmap.llm.dumpIo": {
          "type": "boolean",
          "default": false,
          "description": "Write LLM prompts and responses under <workspace>/agent-mindmap-llm-dumps/ (live CLI, llm-cache, library-cache, ontology-cache)."
        },
        "agentMindmap.llm.dumpDir": {
          "type": "string",
          "default": "",
          "description": "Override LLM dump directory. Empty = workspace/agent-mindmap-llm-dumps, or ~/.agent-mindmap/agent-mindmap-llm-dumps if no workspace folder is open."
        },
        "agentMindmap.storeDir": {
          "type": "string",
          "default": "",
          "description": "Path to the cross-project analysis library directory. Empty = ~/.agent-mindmap. Supports a leading ~/. Point at a sync folder (iCloud / Dropbox) to share the library across machines."
        },
        "agentMindmap.library.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Persist each analysed session to storeDir as a SessionRecord with meta, and skip the LLM call on reopen if the transcript is unchanged. Disable to fall back to the old globalStorage hash-only cache."
        },
        "agentMindmap.library.batchRefineOntology": {
          "type": "boolean",
          "default": true,
          "description": "During batch analyze, incrementally reuse ontology nodes/mappings, infer topic paths for new sessions only, and refine segment equivalences (refine v3: outline upstream/downstream + sibling context) after each batch and when a single session is added."
        },
        "agentMindmap.library.batchFinalRefine": {
          "type": "boolean",
          "default": true,
          "description": "After all sessions in a batch analyze run finish, refresh the merge snapshot and concept trie (DET only; no extra M-merge LLM call)."
        },
        "agentMindmap.library.mergeMode": {
          "type": "string",
          "enum": [
            "full",
            "delta"
          ],
          "default": "delta",
          "description": "Batch concept merge: delta (default) = batch 1 full on milestone sessions, batch 2+ snapshot delta only. Setting full does not trigger all-session or periodic M-merge on batch 2+ (see merge-snapshot-delta rule)."
        },
        "agentMindmap.library.mergeFullReconcileEvery": {
          "type": "number",
          "default": 4,
          "minimum": 1,
          "description": "Deprecated — ignored. Periodic full reconcile (every N batches) is forbidden; only batch 1 uses full M-merge."
        },
        "agentMindmap.library.incrementalOntologyOnSessionAdd": {
          "type": "boolean",
          "default": true,
          "description": "When a single session is analyzed and saved to the library, incrementally update topic paths and refine segment equivalences for the concept mind map (same as batch incremental ontology)."
        },
        "agentMindmap.merge.autoRebuildDeterministic": {
          "type": "boolean",
          "default": true,
          "description": "After a session is analysed and written to the library, automatically rebuild storeDir/merges/deterministic.json so 'Open Merged View' opens instantly."
        },
        "agentMindmap.ui.preset": {
          "type": "string",
          "enum": [
            "auto",
            "dark",
            "light"
          ],
          "default": "auto",
          "description": "Mind map color preset. auto follows the editor theme; dark and light use mind-elixir built-in themes."
        },
        "agentMindmap.ui.locale": {
          "type": "string",
          "enum": [
            "auto",
            "en",
            "zh-cn",
            "ja",
            "ko"
          ],
          "default": "auto",
          "description": "Language for extension notifications and prompts (when agentMindmap.llm.promptLanguage is auto). auto follows Configure Display Language. ja/ko require a community-contributed translation bundle — falls back to English when missing."
        },
        "agentMindmap.llm.promptLanguage": {
          "type": "string",
          "enum": [
            "auto",
            "en",
            "zh"
          ],
          "default": "auto",
          "description": "Language used in LLM prompts. auto follows agentMindmap.ui.locale (zh-cn → zh; en/ja/ko → en). Override to force a specific prompt language regardless of UI locale. Note: changing this invalidates the SessionRecord cache."
        },
        "agentMindmap.ui.direction": {
          "type": "string",
          "enum": [
            "side",
            "side-lr",
            "left",
            "right"
          ],
          "default": "side",
          "description": "Mind map layout direction: both sides with first branch on the right (side, default), both sides with first branch on the left (side-lr), left only, or right only."
        },
        "agentMindmap.ui.themeFile": {
          "type": "string",
          "default": "",
          "description": "Optional JSON file overriding palette and CSS variables (cssVar). Empty = no file. Supports workspace-relative paths, ~/..., or absolute paths."
        }
      }
    },
    "configurationDefaults": {
      "workbench.editorAssociations": {
        "*.md": "vscode.markdown.preview.editor"
      },
      "workbench.diffEditorAssociations": {
        "*.md": "default"
      }
    }
  },
  "scripts": {
    "postinstall": "node scripts/patch-sqlite3-binding.js",
    "build": "node esbuild.config.mjs && esbuild ../test/run-all.ts --bundle --platform=node --alias:vscode=../test/vscode-stub.cjs --external:@vscode/sqlite3 --outfile=dist/test-run.js && node ../scripts/build-eval-run.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "test": "node dist/test-run.js",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^20.14.10",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.2.1",
    "esbuild": "^0.28.0"
  },
  "dependencies": {
    "@vscode/sqlite3": "^5.1.12-vscode",
    "markdown-it": "^14.1.0"
  }
}
