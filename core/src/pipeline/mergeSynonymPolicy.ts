/** Merge trie: apply segmentEquivalences when inserting paths / collapsing siblings. */
export const MERGE_APPLY_SEGMENT_EQUIVALENCES = false;

/** Merge M2 post-pass: derive DET overlap hints into segmentEquivalences. */
export const MERGE_DERIVE_SEGMENT_EQUIVALENCES = false;
